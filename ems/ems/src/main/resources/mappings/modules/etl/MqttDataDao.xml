<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.etl.dao.MqttDataDao">
	
	<!-- 查询数据 -->
	<select id="findList" resultType="MqttData">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
			<if test="tsStart !=null and tsEnd !=null">
				and a.ts between #{tsStart} and #{tsEnd}
			</if>
			<if test="deviceCodeList != null and deviceCodeList.size() != 0">
				and a.meter_code in (
					<foreach collection="deviceCodeList" item="deviceCode" separator=",">
						#{deviceCode}
					</foreach>
				)
			</if>
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select>

	<select id="getLatestMqttData" resultType="MqttData">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
			<if test="tsStart !=null and tsEnd !=null">
				and a.ts between #{tsStart} and #{tsEnd}
			</if>
		</where>
		ORDER BY a.ts DESC
		LIMIT 1
	</select>
	
</mapper>