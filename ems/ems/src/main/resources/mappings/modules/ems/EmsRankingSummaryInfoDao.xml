<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.ems.dao.EmsRankingSummaryInfoDao">
	
	<!-- 查询数据
	<select id="findList" resultType="EmsRankingSummaryInfo">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<delete id="deleteBySummaryId">
		delete from ems_ranking_summary_info where ranking_summary_id =#{rankingSummaryId}
	</delete>
</mapper>